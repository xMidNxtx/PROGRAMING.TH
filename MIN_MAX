#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    int x, y, min=2e9, max=-2e9;
    cin >> x;
    for(int i=0; i < x; i++){
        cin >> y;
        if(min > y) min = y;
        if(max < y) max = y;
    }
    cout << min << endl << max;
}
